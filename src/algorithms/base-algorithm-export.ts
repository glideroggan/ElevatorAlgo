import { BaseElevatorAlgorithm } from './BaseElevatorAlgorithm';

// Export the base algorithm class to the global scope for use in the sandbox
(window as any).BaseElevatorAlgorithm = BaseElevatorAlgorithm;
