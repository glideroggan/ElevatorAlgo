{
  "name": "elevator-algo",
  "version": "1.0.0",
  "description": "Elevator algorithm simulation game",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc --emitDeclarationOnly && node esbuild.config.js",
    "dev": "node esbuild.config.js --watch",
    "watch": "tsc --emitDeclarationOnly -w",
    "start": "yarn build"
  },
  "dependencies": {
    "p5": "^1.7.0"
  },
  "devDependencies": {
    "@types/p5": "^1.6.2",
    "esbuild": "^0.25.0",
    "esbuild-plugin-copy": "^2.1.1",
    "serve": "^14.2.0",
    "typescript": "^5.1.6"
  }
}
